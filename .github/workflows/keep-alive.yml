name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keep-alive endpoint
        run: curl -f https://mosaed-arabic.vercel.app/api/keep-alive